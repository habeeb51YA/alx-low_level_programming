#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - Prints the alphabel on a new line
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
char dd;
for (dd = 'a'; dd <= 'z'; ++dd)
printf("%c\n", dd);
return (0);
}

